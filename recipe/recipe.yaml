# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.2.0"

package:
  name: fps-file-watcher-poll
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/f/fps-file-watcher-poll/fps_file_watcher_poll-${{ version }}.tar.gz
  sha256: 15ecbd82cf8e0ae6274743f87568cf5b8657663cdce2f9de38b1095d7f89608b

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - uv-build
  run:
    - python >=${{ python_min }}
    - anychange >=0.2.0
    - jupyverse-api >=0.14.0,<0.15.0

tests:
  - python:
      imports:
        - fps_file_watcher_poll
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: An FPS plugin for the file watcher API
  description: An FPS plugin for the file watcher API
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/plugins/file_watcher_poll

extra:
  recipe-maintainers:
    - davidbrochart
